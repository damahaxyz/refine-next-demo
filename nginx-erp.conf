server {
    listen 80;
    server_name erp.tikool.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name erp.tikool.com;

    ssl_certificate /root/ssl/erp/erp.tikool.com.pem;
    ssl_certificate_key /root/ssl/erp/erp.tikool.com.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    # Serve uploaded/generated files directly from storage
    location /products/ {
        alias /root/erp/storage/products/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;

        # Increase body size for file uploads
        client_max_body_size 50m;
    }
}
