services:
  erp:
    build: .
    container_name: erp-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
      - ./storage:/app/public
    env_file:
      - .env.production
